| Tier | ID | File(s) | What Was Wrong | What Was Changed |
| --- | --- | --- | --- | --- |
| TIER 1 | F-001 | `app/api/run/route.ts` | Process execution could fail unexpectedly after the dependency update because the subprocess cancellation option no longer matched the installed `execa` version. | Switched the route to the correct `cancelSignal` option and normalized engine startup, timeout, and missing-binary failures into structured API responses. |
| TIER 1 | F-002 | `lib/apiContract.ts`, `app/api/run/route.ts`, `lib/runTransform.ts` | Request and response validation were handwritten, inconsistent, and easy to drift across client and server boundaries. | Added a shared Zod contract for the run request and response, enforced it server-side, and validated the response again on the client before updating UI state. |
| TIER 1 | F-003 | `lib/commandPolicy.ts`, `app/api/run/route.ts` | Dangerous or unsupported commands could still reach the local execution engine through file-backed operations or host-access DSL helpers. | Added token-aware command policy checks that block file-dependent verbs plus host-access helpers such as `system()`, `exec()`, and `stat()` before execution begins. |
| TIER 1 | F-004 | `lib/validation.ts`, `components/GridcraftStudio.tsx`, `components/OutputPanel.tsx` | Invalid submissions relied too heavily on server feedback, and execution failures were not modeled as a first-class UI state. | Added strict client-side validation for empty input, empty commands, and oversize payloads, and routed every failure path through a visible error surface in the output panel. |
| TIER 1 | F-005 | `components/GridcraftStudio.tsx`, `lib/runTransform.ts`, `lib/clientSession.ts` | In-flight requests could overlap with no cancellation, no request sequencing, and no per-session concurrency guard. | Added `AbortController`, monotonic request ids, client session identifiers, and server-side per-session locking so stale or concurrent responses cannot corrupt the workspace state. |
| TIER 1 | F-006 | `app/error.tsx`, `app/loading.tsx`, `app/page.tsx` | The app lacked resilient route-level loading and error boundaries, leaving blank or brittle failure modes during rendering. | Added explicit loading and error routes and replaced the blank suspense path with a resilient, visible shell. |
| TIER 2 | F-007 | `lib/studioState.ts`, `components/GridcraftStudio.tsx` | The main workspace had classic `useState` soup: too many related state slices, duplicated resets, and brittle status transitions. | Replaced the fragmented state model with a typed reducer, centralized execution transitions, and isolated history, copy status, and preset behavior into one predictable state machine. |
| TIER 2 | F-008 | `components/InputPanel.tsx`, `components/CommandBar.tsx`, `components/OutputPanel.tsx`, `components/OperationsReference.tsx` | Accessibility coverage was incomplete for labels, live regions, and feedback semantics on high-value interactions. | Added explicit labels, `aria-describedby`, live status messaging, alert semantics for errors, and clearer interaction states throughout the workspace. |
| TIER 2 | F-009 | `app/api/run/route.ts`, `next.config.mjs` | The API layer lacked production-grade security posture around response headers and route behavior. | Added CSP, COOP, XFO, content-type, referrer, and permissions headers, plus no-store caching on the execution route. |
| TIER 2 | F-010 | `lib/runMetrics.ts`, `components/OutputPanel.tsx` | Successful and failed runs did not expose enough operational context for users to understand what happened. | Added normalized row counting, run-duration reporting, preview truncation messaging, and consistent status text for running, success, and failure outcomes. |
| TIER 3 | F-011 | `lib/shareState.ts`, `app/page.tsx`, `components/GridcraftStudio.tsx`, `lib/studioState.ts` | URL sharing depended on client-side `useSearchParams()` hydration and could force a fallback render path on first load. | Moved shared-state decoding to the server page, initialized the reducer from the decoded state, compacted the serialized URL payload, and kept an escape hatch for oversized URLs. |
| TIER 3 | F-012 | `components/InputPanel.tsx`, `components/CommandBar.tsx`, `components/OutputPanel.tsx`, `components/OperationsReference.tsx` | Pure presentational components re-rendered on unrelated parent state changes. | Memoized the presentational panels and moved the operation list behind a dynamic import so the main workspace pays the cost only when that UI is needed. |
| TIER 3 | F-013 | `components/VirtualizedOutput.tsx`, `components/OutputPanel.tsx` | Large outputs were still expensive to paint because they rendered as one full block even when the viewport showed only a small slice. | Added simple windowed rendering for large output previews so the app only paints visible rows plus a small buffer. |
| TIER 3 | F-014 | `lib/runMetrics.ts`, `lib/shareState.ts`, `lib/validation.ts` | Core utility logic was duplicated or mixed into the main component, raising complexity on the hottest user path. | Extracted reusable helpers for preview generation, row counting, share-state encoding, and validation so the UI layer stays smaller and easier to reason about. |
| TIER 4 | F-015 | `app/page.tsx`, `components/GridcraftStudio.tsx`, `app/layout.tsx`, `app/globals.css`, `components/CommandBar.tsx`, `components/InputPanel.tsx`, `components/OperationsReference.tsx`, `components/OutputPanel.tsx`, `components/VirtualizedOutput.tsx` | The first paint path was slowed by a client-side search-param suspense boundary and unnecessary local variable-font downloads. | Removed the client suspense dependency by decoding share state on the server and replaced the local font payload with native system font stacks, which improved Lighthouse from 96 to 99 and reduced LCP from 2.75s to 2.18s. |
| TIER 4 | F-016 | `package.json`, `package-lock.json`, `next.config.mjs` | The dependency and tooling stack carried outdated packages, inconsistent lint wiring, and no bundle-analysis path for performance verification. | Upgraded Next.js and lint tooling to current compatible versions, added bundle analyzer support, and verified the production bundle against the final build. |
